<h1>商品一覧</h1>

<% @items.each do |item| %>

  <p>商品名</p>
  <%= item.name %>
  <%= link_to "詳細",admin_item_path(item.id) %>
  <%= link_to "編集",edit_admin_item_path(item.id) %>

<% end %>

<%= link_to "新規登録ボタン",new_admin_item_path %>
