<h1>商品編集</h1>

<%= form_with model: @item,url: admin_item_path(@item.id) do |f| %>

  <h4>商品名</h4>
  <%= f.text_field:name %>

  <h4>商品説明</h4>
  <%= f.text_area:introduction %>

  <h4>税抜き価格</h4>
  <%= f.text_field:price %>

  <%= f.submit '保存' %>

<% end %>